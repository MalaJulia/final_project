import Box from "@mui/material/Box";

const Footer = () => (
  <Box
    sx={{
      backgroundColor: "primary.main",
      height: "50px",
        marginTop:" auto"
    }}
  />
);
export default Footer;
